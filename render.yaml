services:
  - type: web
    name: pcos-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run pcos_app.py --server.port $PORT --server.address 0.0.0.0
